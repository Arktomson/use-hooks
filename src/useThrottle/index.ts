import { useEffect } from 'react';

interface options {
  wait: number;
  leading: boolean;
  trailing: boolean;
}
export const useThrottle = (originFn: () => void) => {
  function throttleFn() {}
  return {};
};
